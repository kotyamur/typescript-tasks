//Тип unknown підходить для сценаріїв, коли ви не знаєте точного типу даних, 
//але все ж таки хочете підтримувати сувору перевірку типів.
//Змінні цього типу слід перевіряти перед їх використанням.

let some: unknown;
some = 'Text';
let str: string;

if (typeof some === "string") {
  str = some; // OK, тепер ми знаємо, що це рядок
}

export {};